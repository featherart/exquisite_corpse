<div class="hero-unit">
<p>
  <h1>New Drawing Pair</h1>
</p>
  <!--display a random head using a Hash, but it's the wrong way!-->
  <%# drawings = Drawing.all
     heads = Hash.new
     random_head_id = 0

     drawings.each do |drawing|
       if drawing.type_id == 1 
         heads[drawing.id] = drawing.image
       end 
     end 
 
     until heads[random_head_id] != nil
       heads.each do |key, value| 
         random_head_id = Random.rand(key)         
       end
     end %>

  <!--<img src="<%#= heads[random_head_id]  %>"></src>-->
  <%#= link_to image_tag("transparent.png", alt: ""), '#' %>
  <!--<div style="width: 500px; height: 20px; overflow: hidden">
  <img class="crop" src="<%#= @randomly_selected_head.image %>" width="500" height="20"></src>
</div>-->

<% 
    if params[:pair_type].to_i == 1  || params[:pair_type].to_i == 3 
%>
    <div class="crop-top">
      <img src="<%= @randomly_selected_drawing.image %>">
      <%= render 'form' %>
<%  
    end
%>
  
<% 
    if params[:pair_type].to_i == 2  || params[:pair_type].to_i == 4 
%>

    <div class="crop-bottom">
      <%= render 'form' %>
      <img src="<%= @randomly_selected_drawing.image %>"> 
<%  
    end
%>
  
</div>

<%#= link_to 'Back', pairs_path %>

